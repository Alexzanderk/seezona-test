import React from 'react';
import FormContainer from '../../containers/Form';
import './style.sass';

const Form = () => {
	return (
		<div>
			<FormContainer />
		</div>
	);
};

export default Form;
